[pytest]
testpaths = tests
markers =
    slow: invokes the R simulator; takes tens of seconds
    benchmark_data: requires the separately generated full benchmark datasets
    requires_r: requires an R installation
